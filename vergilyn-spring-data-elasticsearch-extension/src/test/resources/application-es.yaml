vergilyn:
  es:
    uris:
      - 127.0.0.1:9200
    http-client-config:
      max-conn-per-route: 10
      max-conn-total: 30

## logging `org.elasticsearch.client` request/response
logging:
  level:
    tracer: trace
  pattern:
    dateformat: HH:mm:ss.SSS


